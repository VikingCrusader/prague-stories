# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Commands

```bash
# Backend (run from server/)
npm run dev          # nodemon on port 5000
npm run start        # production start
npm test              # Jest unit + controller integration tests (mongodb-memory-server)

# Frontend (run from client/)
npm run dev          # Vite dev server on port 5173
npm run build        # production build
npm test              # Jest unit/component tests
npm run test:e2e      # Playwright e2e (client/e2e/), mocks the backend via page.route

# Data scripts (run from server/)
node src/data/seedLocations.js    # upsert seeded location cards (idempotent)
node src/data/seedHistoryEvents.js # upsert History Timeline events (idempotent, $setOnInsert — see caveat below)
npm run export:locations          # export DB → static seed files
npm run sync:covers               # sync Cloudinary cover images to local

# Data scripts (run from client/)
npm run generate:covers           # rescan public/pixel-art/ → rebuild src/utils/coverManifest.json
```

There is no linter configured.

### Testing

- Server tests live in `server/__tests__/`. `rarityMap.test.js` and `gamification.test.js` are pure-function unit tests. `authController.test.js`, `locationController.test.js`, `checkinController.test.js`, `userController.test.js`, and `historyController.test.js` are integration tests that spin up an in-memory MongoDB (`mongodb-memory-server`) and drive the real Express app via `supertest` — they exercise actual Mongoose behavior (unique indexes, populate, lean) rather than mocking the models.
- `server/src/app.js` exports the Express app with no `listen()` call; `server/src/index.js` is the thin entry point that connects to Mongo and starts listening. Tests import `app.js` directly so they never need a real `MONGO_URI` or open a port.
- External calls (Cloudinary upload, Gemini description generation) are mocked in tests via `jest.unstable_mockModule`, not exercised for real.
- Client tests: `client/src/__tests__/` (Jest + RTL) for units/components, `client/e2e/` (Playwright) for full-page flows with the backend mocked via `page.route`.

## Architecture

This is a MERN monorepo (`client/` + `server/`) with no shared package between them.

### Backend (`server/src/`)

Express app with ES modules (`"type": "module"`). Entry point is `index.js` — mounts five route groups:

- `/api/auth` — register, login, `GET /me`
- `/api/locations` — CRUD + cover upload; GET routes use `optionalAuth` (populates `req.user` when a JWT is present but doesn't block unauthenticated requests); write routes use `protect`
- `/api/checkins` — check-in, history (no undo — check-ins are permanent once made)
- `/api/user` — XP progress, achievements
- `/api/history` — History Timeline events, read-only (`optionalAuth`, no write routes — content is hand-curated via a seed script, not user-submitted; see **History Timeline** below)

**Key patterns:**
- Location slugs are the canonical identifier throughout (not `_id`). All routes are `:slug`-based.
- `GET /api/locations` excludes the `description` field (too heavy for the grid). `GET /api/locations/:slug` includes it and triggers **lazy Gemini description generation** if any language is missing — it generates, saves to DB, and returns in the same request.
- GPS distance validation happens in the checkin controller. In `NODE_ENV=development` the check is skipped entirely so local testing works without being in Prague.
- Rarity and XP are coupled: `RARITY_XP` in `data/rarityMap.js` is the single source of truth. `createLocation` and `updateLocation` both call it to keep `xpReward` in sync with `rarity`.

**Models:**
- `Location` — `name` (EN display), `slug`, `localizedNames: { cz, zh }`, `labels[]`, `coordinates: { lat, lng }`, `description: { en, cz, zh }`, `rarity`, `xpReward`, `wikipediaUrl`, `coverImage`, `pixelArtKey`, `addedBy` (`null` = preset, `ObjectId` = user-added)
- `CheckIn` — `user`, `location` (ObjectId ref), `createdAt`
- `User` — `username`, `email`, `passwordHash`, `xp`, `achievements[]`
- `HistoryEvent` — `slug`, `era` (key into `data/historyEras.js`), `startYear` (numeric sort/timeline anchor, negative = BCE, not localized — a bare number reads fine in any language), `title`/`hookLine`/`summary`/`year: { en, cz, zh }` (`year` is the localized display string, e.g. `"935 (traditional date)"` / `"935 (tradičně uváděný rok)"` / `"935年（传统纪年）"`), `tone` (`humorous`/`serious`, stored but not yet read by the frontend), `quotes: [{ text: {en,cz,zh}, attribution: {en,cz,zh} }]` (primary-source quotes, referenced from inside `summary` via a `"[[quote:N]]"` marker on its own paragraph — see **Primary-source quotes** below), `relatedLandmarks: [{ slug, relation: { en, cz, zh } }]`, `wikipediaUrl`, `images` (array of static paths, e.g. `['/history/prehistory.webp']`), `imageCaptions: [{en,cz,zh}]` (optional small caption rendered under each image, matched to `images` by array index — an entry can be left `{}` for an image that doesn't need one). `summary` text may also contain inline `"[[link:slug]]display text[[/link]]"` markers — see **In-text cross-reference links** below.

**Gamification** (`services/gamification.js`): `LEVELS` array and `ACHIEVEMENTS` array are the authoritative definitions for all level thresholds and achievement logic. `evaluateAchievements` runs on every check-in. Achievements fire once and are stored on the User document.

**Adding new locations (`data/seedLocations.js`):**
- Every entry needs a full `description: { en, cz, zh }` — all three languages, not just `en`.
- English description structure: an opening paragraph of ~50 words that introduces the location simply and humorously, followed by one or two main-text paragraphs with the substantive detail, followed by a final Bonus paragraph. CZ and ZH mirror this structure.
- Do not include Czech-name/place-name translations anywhere in the EN or ZH description text — in particular, no parenthetical Czech-translation note in the EN/ZH opening paragraph (e.g. no `Welcome to X (Český název)!` pattern).
- **History Timeline and Location descriptions are two separate content types — don't blur them (confirmed 2026-08-29).** A Location's `description` should not reference the History Timeline as a shared narrative the reader has supposedly already progressed through — no `"the Hussite general this project has already met never losing a single battle"`-style phrasing (`kost-castle`'s original draft), no `"this timeline has already told"` callbacks, no assuming the reader has read a specific History Timeline event. A Location card is meant to stand on its own; if a figure or event also appears on the History Timeline, that's a coincidence of subject matter, not a excuse to write as if the two features share one continuous story. (Some earlier entries — `bezdez-castle`, `zricenina-hradu-rotstejn`, `liberec` — already have this kind of cross-reference baked in from before this rule was confirmed; not retroactively fixed unless the user asks, but don't add more.)
- Don't add a `landmark` label to any new entry.
- `rarity`: `legend` is reserved for genuinely monumental/grand/massive buildings — usable for a new entry, not just the original preset landmarks, but sparingly. **Scope update (2026-08-22):** the original "never `legend` for new entries" rule was set when new locations were still Prague-only, where few buildings left to add were actually grand enough — now that new locations span all of Bohemia, a sufficiently monumental building anywhere in Bohemia (e.g. `zamek-hluboka-nad-vltavou`) can justify `legend` too. Default to `common`/`rare`/`superior`/`epic`/`mythic` for everything else. `xpReward` stays in sync automatically via `RARITY_XP` in `data/rarityMap.js`.
- `coordinates` are supplied by the user for each new location — don't look them up.
- **Scope rule for non-Prague locations (confirmed 2026-08-23): this app is "Prague Stories" — a city outside Prague only ever gets *one* Location card, treated as a single day-trip destination, not a second city to build out landmark-by-landmark.** Don't create a separate card for every notable sight within that same non-Prague city (e.g. a second statue, a second church) — fold anything else worth mentioning into that one city card's own description instead (as a sentence in its tourism paragraph, the way `ceske-budejovice`'s founder statue is handled) or into the History Timeline event's own prose, not as its own `relatedLandmarks` entry. The one standing exception is an out-of-town castle/monastery some distance from its nearest town (e.g. `bezdez-castle`, `zamek-hluboka-nad-vltavou`) — those can still get their own card even when a nearby town already has one. **Locations outside the Czech Republic never get a card at all** (e.g. Marchegg, Austria) — mention them only in running prose, never as a Location. **Second exception, confirmed 2026-08-24 (`vlassky-dvur`, `kostel-svate-barbory`, `kostnice-sedlec`, all in/around Kutná Hora):** the user's own stated two-part test for this specific exception — "库特纳霍拉是例外，因为离布拉格够近+旅游景点够硬，我才拆出来的" — a town has to clear **both** bars: (1) genuinely close enough to Prague to function as a satellite/day-trip town, not a distant second city, **and** (2) the individual sub-landmark itself has to be a serious, independently-major attraction in its own right (a real museum/monument people specifically travel to see — Kutná Hora's mint, cathedral, and ossuary all separately draw visitors), not just any notable building inside an already-carded town. Both conditions together, not either alone. This is a judgment call, not a blanket reopening of the one-card rule — flag it to the user rather than assuming it applies elsewhere, even for other Prague-adjacent towns (see `kutna-hora`'s own card, which still folds St. Barbara's Cathedral's basic facts into its own description even after St. Barbara's got a separate card, since the two serve different purposes: the town card's quick mention vs. the landmark card's full depth).
- **Third exception, confirmed 2026-08-24 (`olomouc`, and `brno`/`slavkov-u-brna` flagged for when they come up): a city can earn its own card purely on "just barely a Prague day trip + outsized standalone historical weight" grounds, without needing a Kutná Hora-style sub-landmark split.** User's own reasoning for Olomouc: close enough to still nominally count as a day trip from Prague, and it's the site of Wenceslas III's 1306 assassination (a major History Timeline event) plus its own separate deep bench of later history (Thirty Years' War Swedish occupation, the 1758 Prussian siege, 1848 imperial abdication). Brno was named in the same breath as a second, not-yet-built example, with Slavkov u Brna (Austerlitz, ~200km from Prague — well past the usual distance norms) flagged as a further one to revisit once the Napoleonic era is reached. **The house style for this exception is the opposite of the Kutná Hora pattern: write ONE long, rich card for the city itself (the `plzen`/`jihlava` template — intro paragraph, historical development, tourist landmarks folded into the same card's prose, closing bonus) rather than splitting named sub-landmarks into their own separate Location entries.** `olomouc`'s own card folds in its Holy Trinity Column, St. Wenceslas Cathedral, the Přemyslid bishop's palace, and its astronomical clock this way — none of those got (or should get) their own card.
- After writing the new entries, run `node src/data/seedLocations.js` (from `server/`) to upsert them into the database — just run it once the content is done, no need to ask first.

**History Timeline** (`models/HistoryEvent.js`, `data/historyEras.js`, `data/seedHistoryEvents.js`, `controllers/historyController.js`): a second, separate content type from Location — hand-curated historical events grouped into eight eras, browsable at `/history` on the frontend. `data/historyEras.js` is a static config (same reasoning as `RARITY_XP` in `rarityMap.js` — small, rarely-changing enum, not worth a DB collection) listing all eras with `key`, `order`, `themeClass`, `title: { en, cz, zh }`, `yearRange`, `tagline`, and `hasContent` (flip it manually once an era's events are seeded). As of 2026-08-23, `legends-origins`, `bohemian-duchy`, and `rise-of-a-kingdom` have real content; `kingdom-golden-age` and everything after are still placeholders. **Era 3/4 split (2026-08-23):** what used to be one era, `kingdom-golden-age` spanning 1199–1378, got split into two on the observation that "Bohemia's golden age" in actual historiography means Charles IV's reign specifically (1346–1378) — not the 148 years before it (Otakar II's rise-and-collapse, the Wenceslas II guardianship crisis, the 1306 Přemyslid extinction, John of Luxembourg's own reign as a rarely-in-Bohemia "wandering king" mortgaging crown lands rather than building anything lasting). `kingdom-golden-age` kept its key and now refers only to 1346–1378 (`hasContent` flipped back to `false` until real Charles IV events exist); the new era `rise-of-a-kingdom` covers 1199–1345 and inherited all ~32 previously-seeded events from the old single era (none of them post-date 1345). **`yearRange` corrected 2026-08-26 to 1199–1346:** the era's own content later grew to include John's 1346 death and Charles's 1346 King-of-the-Romans election, kept as `rise-of-a-kingdom`'s own closing bookend at the user's explicit direction rather than moved to `kingdom-golden-age` — the two eras now deliberately share 1346 as a boundary year, not by mistake. `GET /api/history` returns `{ eras, events }` in one payload — no list/detail split like `/api/locations`, since event copy is a few short hand-written sentences rather than a heavy lazily-generated description. Each event's `relatedLandmarks` stores bare Location slugs; the controller resolves them against the live `locations` collection in one batched query, enriches each with the full Location document (`-description` projection) plus a per-request `unlocked` flag computed the same way `getLocations` does it (via `CheckIn` lookup against `optionalAuth`'s `req.user`), and **silently drops any slug that doesn't resolve** rather than sending the client a dead link.

**Adding new history events (`data/seedHistoryEvents.js`):**
- Every event needs `title`, `hookLine` (one-sentence hook), and `summary` (2–3 sentences) in all three languages.
- **Voice**: default to wry, deadpan comic history-writing in all three languages — short punchy sentences, concrete specific details kept rather than smoothed into abstraction, and a dry closing line that reframes the facts as a joke. Model paragraph (`vysehrad-revival-1070`, the 1075 Langensalza / 1077 "Gebhard" aside): *"两人较劲用了不同的招——一个靠打仗立功，一个靠改名讨好皇帝——看来'想方设法气对方'才是这家人真正代代相传的东西。"* That's the target register for pre-modern content generally, not just this one event. Exception: modern-era atrocity/mass-death content (e.g. Lidice) stays solemn — same format, drop the jokes, not a Lidice-only rule. **Broader 20th-century pullback (confirmed 2026-08-24):** once content reaches the eras `brief-independence` (1918–1938), `nazi-nightmare` (1938–1945), and `cold-war-sorrow` (1946–1992) — see `data/historyEras.js` — default to a more restrained, measured voice generally, not just for individual atrocity-tagged events within them. Dry irony and understatement can still land (e.g. an era tagline noting Czechoslovakia's allies "hand it over to appease the neighbor next door"), but drop the punchy joke-closer structure that defines the earlier-era default, especially for anything touching occupation, violence, or repression directly. The material carries more sustained weight than earlier eras; the wit shouldn't be the default assumption that then gets card-by-card overridden.
- **Dash frequency (confirmed 2026-08-25):** em-dashes (`—`) had crept into overuse across all three languages, and the density itself makes sentences feel long and hard to read — pull back the frequency in new History Timeline prose, EN/CZ/ZH alike. Prefer a plain comma, a period and a new sentence, a colon where the aside is doing real explanatory work, or parentheses for a genuine aside/qualifier. **Not a ban** — the user confirmed explicitly ("也不是完全不能用破折号，但是要在不得不用的时候才用"): treat it as a last resort, reached for only when none of those alternatives actually works, not as one option among several to pick freely. This is a general house-style tightening, not specific to one language (compare [[feedback_natural_sentence_style]], which flagged the same tic as a ZH-specific "AI tell" — that observation still stands, but the fix now applies everywhere).
- **Avoid the "this timeline" self-reference** (confirmed 2026-08-30, user said it reads badly and explicitly dislikes it): don't write card copy that breaks the fourth wall to talk about "this timeline" as a narrating entity — "this timeline already covered...", "这条时间线已经讲过...", "tahle časová osa už..." — even though this exact device had been in near-constant use across the whole file before this date (roughly 70–80 occurrences at the time it was flagged). Prefer a [[link:slug]] cross-reference (see below) when the callback target has its own card, or just rephrase the callback to an in-story actor instead of the meta-narrator — e.g. "Charles's own court had already worn out the story it was so eager to spread" rather than "this timeline has already told the story Charles's own court was so eager to spread." This is confirmed as a going-forward default for all new/edited copy; the large stock of already-shipped cards using the old device was NOT swept retroactively as part of this fix — only ask about a full retroactive pass if the user raises it, don't assume it's wanted.
- **Vary the forward-hook closer** (confirmed 2026-08-25): a card that deliberately doesn't finish its own story (leaving the actual next event for its own card) needs some closing line gesturing at what's coming, but "接下来发生的事，是另一个故事了" ("What came next is its own story") had become a reflexive default across the Rise-of-a-Kingdom arc — used on `crowned-twice-1307`, `seven-kings-then-none-1306`, `thoroughly-incompetent-king-1307`, `the-fleeing-princess-1310`, and others in a row. Don't lean on this one stock phrase every time. Vary the construction: name what's coming without narrating it ("what he did with it is its own story"), end on an image or irony instead of a meta-statement about "the story," pose it as an open question, or just stop cold on the last concrete fact and trust the reader to feel the cliffhanger without a signposting sentence at all. The underlying job (close this card without spoiling the next one) stays the same; the sentence doing that job shouldn't read identically card after card.
- **Primary-source quotes** (`quotes: [{ text: {en,cz,zh}, attribution: {en,cz,zh} }]`, added 2026-08-24): when `summary` needs to quote a chronicle or historian directly (the Zbraslav Chronicle, Palacký, Henry of Heimburg, etc.), don't just embed the quote inline as quoted prose inside a regular paragraph — pull it into its own `quotes[]` entry and drop a `"[[quote:N]]"` marker on its own line (surrounded by blank lines like any other paragraph break) at the point in `summary` where it should appear. **Proactively suggest one, don't wait to be handed the quote text** — when researching an event and a genuine primary-source line or a notable historian's own verdict turns up (in a chronicle, in a WebSearch result, anywhere), pull it into this format on your own initiative, the same way background-card and territory-map opportunities already get flagged unprompted elsewhere in this doc. **This means actively searching for one, not just noticing if one happens to surface** (confirmed 2026-08-25): whenever the user hasn't supplied chronicle text themselves, spend a WebSearch pass or two specifically looking for a primary-source line before finishing the card — if a first search comes up empty, a second, differently-worded pass is worth trying before concluding there's nothing (a real case: the first search for Elizabeth's 1310 escape in `the-fleeing-princess-1310` missed the actual chronicle passage entirely; a second, differently-phrased search found it, and it turned out more accurate than the modern-secondary-source detail already drafted into the prose). If a proper search genuinely turns up nothing quotable, that's fine — don't force a weak or tangential quote in just to have one. `HistoryEventSection.jsx` swaps that marker for a distinct blockquote — bordered box, large decorative quotation marks, italic text, right-aligned "— Source" attribution — instead of a plain `<p>`. Every inline quote that existed in the Rise-of-a-Kingdom arc as of 2026-08-24 (the Palacký quote, the "divine vengeance" Zbraslav quote, Henry of Heimburg's famine account, the Continuator of Cosmas welcome-scene quote, the Zbraslav "pleased the Bohemian king" line) has since been retroactively converted to this format at the user's request — this is now the standing convention for all quotes, not just new ones. **Gotcha when converting an inline quote:** check whether the original prose continued in the same paragraph right after the closing quote mark (no blank line) — if so, the marker needs an explicit blank line inserted after it too, or the marker paragraph ends up with trailing text glued onto it and fails the `^\[\[quote:(\d+)\]\]$` match (rendering as a broken visible-marker plain paragraph instead of a blockquote). `node --check` won't catch this; only visually re-checking each `[[quote:N]]` occurrence's surrounding context will.
- **Background-knowledge cards** (`cardType: 'background'`, default is `'event'`): a non-dated explainer aside slotted into the feed at a specific point, for context a reader needs right there but that isn't itself a dated happening — e.g. `bohemia-not-one-city-1101`, inserted between `duke-go-round-1092` and `battle-of-chlumec-1126` to explain that 11th–12th-century Bohemia had several rival power centers (Prague Castle, Vyšehrad, Olomouc/Brno/Znojmo in Moravia, plus assorted local strongholds) rather than one obvious capital, right before a battle whose whole premise depends on that. Renders with a distinct dashed-gold-border look and a collapsible "Background" tag instead of the normal year/title-as-wiki-link header (see `HistoryEventSection.jsx`, `history.css`), and — critically — does **not** claim a year slot in `HistorySidebar`'s nav list (filtered out there and in `HistoryPage`'s scroll-spy) even though it still needs a `startYear` to sort into the right place in the feed. **Proactively suggest one** whenever a piece of genuinely useful but likely-obscure context comes up that doesn't fit inside any single dated event — a European-history convention worth spelling out, a neighboring country's or the wider Empire's political backdrop, a term or institution the reader needs defined before the next few events make sense. Don't wait to be asked; flag the opportunity the way the illustration-prompt and territory-map offers already get flagged (see the reminders elsewhere in this doc). **Hard rule, not just a default: a background card never gets an `image` and never gets a non-empty `relatedLandmarks`** — it's supplementary knowledge only, full stop. Confirmed explicitly by the user 2026-08-22 after an illustration and three landmark links were added to a background card and had to be stripped back out. If a background card's topic naturally connects to a real landmark, hold that link for later and attach it to an actual dated event instead — don't attach it here. **Before writing one, also check whether the subject is going to become a protagonist of its own later** rather than staying background forever — e.g. the Vítkovci family (Rožmberk branch), name-dropped starting in Era 3's `iron-and-golden-king-1266` (Otakar founding `zlata-koruna`/`ceske-budejovice` to counterbalance them, confiscating `jindrichuv-hradec` from them) but deliberately *not* given a background card there (decided 2026-08-22): the Rožmberks become one of the most important noble families in later Bohemian history (Český Krumlov, active into the Hussite/Renaissance/Baroque eras, extinct 1611) and will very likely anchor their own dated event(s) later, where a proper introduction lands with more weight than a background aside now. A passing mention with no card is fine when this is true; don't spend a background card explaining someone who's about to get their own spotlight. **Update 2026-08-23: a short introductory background card now exists — `who-are-the-vitkovci-1276`**, inserted right after `enemies-on-every-side-1276`. Deliberately light: Vítek of Prčice's origins as a Přemyslid court official (cup-bearer 1165, seneschal 1169–75) turned South Bohemia/Austria-border landholder, his sons' split into the "Five Roses" branches (Krumlov, Rožmberk, Hradec, Landštejn, Stráž), and the general loyal-official-vs-rival tension — no mention of Záviš's later marriage to the widowed Kunigunda, his 1290 execution, or the Rožmberk branch's own later peak. **The deeper/fuller version is still deferred, standing reminder unchanged:** proactively flag it when the Rožmberk family specifically becomes a genuine protagonist of their own (Hussite-era Oldřich II in Era 4; Vilém/Petr Vok and Český Krumlov later still) — the user asked to be told, not to have to ask again. **When that fuller card gets written, explicitly callback to where this thread started** — this short 1276 introduction, Záviš of Falkenstein's revolt, the Zlatá Koruna/České Budějovice foundations built specifically to contain this family, and the Jindřichův Hradec confiscation — don't just reintroduce the family cold; tie the payoff back to the setup already on the record.
- `era` must match a `key` in `data/historyEras.js`. `startYear` is the numeric sort/timeline-position anchor (negative = BCE) — for legendary/undated content, anchor it to the era's own conventional/narrative dating rather than a stricter archaeological date if the two diverge, so the timeline still reads in the intended story order (see the comment on `founding-of-vysehrad` for the actual case this came up).
- `relatedLandmarks` slugs must already exist in the `Location` collection — an unresolved slug is silently dropped by the controller, not an error you'll see. **Query the live DB, not just `seedLocations.js`** — a meaningful number of real, correctly-content-filled Locations exist only in the database (user-submitted through the app's own add-location flow, or added by an earlier script/session) and never made it into the seed file at all (`st-georges-basilica`, `namesti-miru`, `basilika-sv-petra-pavla`, and several `*-<random-digits>` user-submitted slugs among them as of 2026-08) — grepping the source file alone will produce false negatives. When searching by name, watch for Czech diacritics that look similar but aren't the same character (e.g. `í` vs `ě` in "Děvín" — a regex written for the wrong one silently returns nothing, not an error).
- **Known caveat, `prague-castle`/`st-vitus-cathedral` FIXED 2026-08-26:** these two Location slugs used to have swapped names (slug `prague-castle` held the genuine St. Vitus Cathedral content, slug `st-vitus-cathedral` held the genuine Archbishop's Palace content) — not a content bug, a slug-naming bug, discovered while wiring up `dawn-over-prague-1344`'s landmarks. Fixed by renaming both docs to their correct slugs (`st-vitus-cathedral` → the cathedral, `archbishops-palace` → the palace, freeing `prague-castle` entirely — a genuine "whole complex" card, if one ever gets written, needs fresh content, not this reclaimed slug) and updating every downstream reference: the 4 `relatedLandmarks` entries pointing at the old `prague-castle` slug across `seedHistoryEvents.js` (`wenceslas-life-and-reign`, `prague-bishopric-973`, `bretislav-succession-law-1055`, `vratislaus-ii-first-crown-1085`), the `castle_conqueror` achievement check in `gamification.js`, and the local cover image filenames in `client/public/pixel-art/` (regenerate `coverManifest.json` after any similar rename, don't hand-edit it). The Archbishop's Palace doc is DB-only, never made it into `seedLocations.js` — same "DB may hold docs the seed file doesn't" gotcha noted elsewhere in this file. **`vysehrad` is still mismatched** (named "Leopold Gate") and remains unfixed — still avoid linking to that one until it gets the same treatment.
- **Don't assume the DB matches what you seeded, even minutes later.** Twice in one session (2026-08-16), Location docs drifted from their just-seeded `rarity`/`labels`/`coordinates` shortly after creation, and a legitimate location once existed twice under two different slugs for the same physical place (`bazilika-sv-ludmily` vs. the pre-existing `namesti-miru`, the latter carrying a garbled, wrong-voice AI-generated description with stray pre-schema fields like `category`/`difficulty`/`isPreset`) — root cause undetermined in both cases. Before publishing new content that links to or duplicates a landmark, query the live doc directly (`Location.findOne`) rather than trusting the seed file or your own last edit.
- Chinese text follows the same rule as Location descriptions: no parenthetical foreign-language name glosses (e.g. no `莉布谢（Libuše）`) — just the Chinese name. A genuine content aside (not a name translation) is fine to keep in parentheses.
- **Title translation across languages doesn't need to be literal — different languages are allowed their own idiomatic phrasing (e.g. `the-crown-he-didnt-win-1254`'s ZH title is a Chinese meme riff with no direct EN/CZ equivalent) — but if an edit to one language's title changes its meaning significantly, the other two languages' titles must be updated to match that new meaning, not left pointing at the old one.** Confirmed explicitly by the user 2026-08-22 after `peace-with-cousin-bela-1254`'s ZH title was changed from a direct translation of the EN/CZ ("与堂兄的和解") to a differently-framed one ("与匈牙利六年的和平") and the user asked for EN/CZ to be brought in line ("Six Years of Peace With Hungary" / "Šest let míru s Uhrami") rather than left as the old framing.
- `image` is optional — a static file dropped into `client/public/history/` and referenced by its `/history/<file>.webp` path; renders as a banner between the summary and the Wikipedia link when present. A small caption line under a specific image is optional too (`imageCaptions[i]`, added 2026-08-30, matched by index to `images[i]`) — leave that index's entry as `{}` for an image that doesn't need one; not every image needs a caption.
- **In-text cross-reference links** (`"[[link:slug]]display text[[/link]]"`, confirmed 2026-08-30): when `summary` prose needs to point at another event's own section — something the reader already met earlier, or a term/institution defined in its own background card — wrap the actual named phrase (already written per-language, not a separate label) in this marker instead of just writing an unlinked callback like "as this timeline already covered." `HistoryEventSection.jsx`'s `renderInlineLinks` turns it into an underlined, clickable inline span that smooth-scrolls straight to that other event's section (same mechanism `HistorySidebar` uses, via `HistoryPage`'s `scrollToSlug`). **This explicitly replaces the old habit of just restating "正是这条时间线先前讲过的那样" (or its EN/CZ equivalents) as inert prose** — reach for the link instead whenever the referenced content already exists as its own event or background card in this file. The surrounding sentence can still keep the "already covered" framing for orientation; the linked span is the actual named thing, not a generic pointer word like "here." Distinct from `[[quote:N]]` — that one replaces a whole paragraph with a blockquote; this one sits inline mid-sentence and wraps text that's already there.
- After writing new entries, run `node src/data/seedHistoryEvents.js` (from `server/`) to upsert them — just run it once content is done, no need to ask first. It's `$setOnInsert`-based like `seedLocations.js`, so **editing an already-seeded event's fields in the source file does not update the live DB** on a re-run — sync a live edit manually (e.g. `HistoryEvent.updateOne({ slug }, { $set: { ... } })`) or delete-and-reseed.

### Frontend (`client/src/`)

React 18 + Vite SPA, deployed to Vercel. `vercel.json` lives in `client/` (not repo root) for SPA rewrite rules to apply.

**Routing** (`App.jsx`): All routes use `<ProtectedRoute guestOk>`. `ProtectedRoute` auto-calls `continueAsGuest()` via `useEffect` when a `guestOk` route is accessed without a user session — so unauthenticated visitors land in guest mode instead of being redirected to `/login`.

**State:**
- `AuthContext` — `user` (JWT-authenticated), `guest` (sessionStorage flag), `loading`. The `guest` flag controls UI differences (no collect button, no add location, guest-specific titles).
- `LanguageContext` — `lang` (`en`/`cz`/`zh`), `zhVariant` (`cn`/`tw`). `useT(key)` is the translation hook used everywhere. `useConvert()` applies opencc-js for Traditional Chinese conversion. Language persists in `localStorage`.

**API layer** (`services/api.js`): Single axios instance pointing to `localhost:5000` in dev and `prague-stories-api.onrender.com` in prod. JWT attached via request interceptor from `localStorage`.

**i18n:** All UI strings live in the `T` object in `LanguageContext.jsx`. Location display names follow the rule: `name` IS the English display field; generic terms (church, bridge) are translated, proper place/person names stay Czech. `localizedNames.cz` and `localizedNames.zh` are always set alongside any change to `name`.

**Location descriptions:** The `description` field on list endpoints is excluded. `LocationDetail` fetches the single-location endpoint to get the full object including descriptions. The lazy Gemini fallback on the server means descriptions populate on first open.

**Pixel art:** `utils/pixelArtMap.js` maps a `pixelArtKey`/label to an emoji, used as the last-resort fallback when no image loads at all. New cards get pixel art generated externally and dropped into `client/public/pixel-art/`; the map entry must be added manually.

**Cover images (local-first):** Location covers load from `client/public/pixel-art/` before ever touching Cloudinary, to keep image traffic off Cloudinary's quota — Cloudinary stays fully wired up as the fallback/upload backend, just not the default read path. `utils/coverManifest.json` (generated by `npm run generate:covers`, see Commands) maps `slug -> filename`, since files are named `<slug>.<ext>` or `<slug>-v<timestamp>.<ext>` (the versioned form is a cache-buster used both by regenerated external pixel art and by the dev-mode cover-upload endpoint, which also patches this manifest directly — see `updateCoverManifest` in `locationController.js`). `utils/localCover.js`'s `getLocalCoverPath(slug)` does the lookup. `LocationCard`, `LocationDetail`, and `MapPage`'s `SidebarDetail` each try, in order: local manifest match → `location.coverImage` (Cloudinary) → emoji fallback (`getArt`). Because local always wins when a manifest entry exists, a cover edited through the app (which only touches Cloudinary + the DB in production — see `uploadCoverImage`) won't visibly update until the local copy is refreshed too: run `node src/data/syncCovers.js --refresh` (server/) to re-download every location's current Cloudinary image over its stale local file (plain `node src/data/syncCovers.js`, no flag, only fills in slugs with no local file at all), then `npm run generate:covers` (client/) to rebuild the manifest. After manually dropping new art into `pixel-art/` outside the upload endpoint, rerun `npm run generate:covers` to pick it up.

**Labels:** The primary label is always first in the array (used for AI description generation fallback). Label strings must match the keys in `LanguageContext`'s `T` object (`label.*`).

**Rarity:** Six tiers: `common / rare / superior / epic / mythic / legend`. XP: 10 / 20 / 30 / 50 / 70 / 100. The `superior` tier colour is `#2c8c03`.

**History Timeline** (`pages/HistoryPage.jsx`, `components/history/`, `styles/history.css`): `/history` renders every seeded event as a continuous vertical feed (`HistoryEventSection`, one per event) next to a sticky sidebar nav (`HistorySidebar`, grouped by era) — a normal scrollable page with persistent navigation, not a click-to-swap detail pane. `HistoryPage` tracks which sidebar entry is highlighted with a plain `scroll` listener (rAF-throttled) that finds the last section whose top has crossed a trigger line near the top of the viewport, not `IntersectionObserver` — an earlier version using the observer's `isIntersecting` entries picked the wrong "active" section when multiple sections partially overlapped the trigger band. Clicking a sidebar entry calls `scrollIntoView({ behavior: 'smooth' })` on the matching section.
  - **Sticky-positioning gotcha:** `.guide-page` (shared by `GuidePage`, `RandomDrawPage`, and `HistoryPage`) sets `overflow-y: auto` in `pixelart.css`. `#root` only has `min-height` (not `height`), so `.guide-page` never actually needs to scroll internally in practice — the real scrolling always happens at the window level — but the `overflow-y: auto` declaration alone still makes it a CSS "scroll container", which silently breaks `position: sticky` on any descendant (sticky computes against the nearest scroll container, and `.guide-page`'s own `scrollTop` never moves). `history.css` overrides it back to `overflow-y: visible`, scoped to `.history-page` only — don't remove that override, and don't add `position: sticky` inside `.guide-page` on another page without adding the same kind of scoped override.
  - **Sidebar z-index gotcha:** `.history-sidebar` sets `z-index: 10` — not decorative. `.loc-card` (used inside the feed's `.location-grid`) is `position: relative`, and both it and the sticky sidebar are otherwise `z-index: auto`, which stack by DOM order — the feed's cards come after the sidebar in the markup, so without an explicit z-index they paint *over* the sidebar as they scroll underneath it. Only visible on mobile (below the `820px` breakpoint), where `.history-layout` stacks to a column and the full-width sidebar and full-width feed share the same horizontal space — on desktop they sit side by side and never visually overlap, so this is easy to break without noticing on a desktop-only check. Don't remove the z-index.
  - Related landmarks render inside the shared `.location-grid` class from `pixelart.css` (so column widths/gaps match Explore's grid), but each grid item is a `.history-landmark-item` wrapper (flex column: `LocationCard`, then its relation caption right underneath) — not a bare card — see `HistoryEventSection`. An earlier version kept cards as direct, unwrapped grid children specifically to preserve CSS Grid's default `align-items: stretch` (uniform card height per row), with captions rendered in a wholly separate second grid below; replaced (2026-08) because captions needed to sit directly under their own card, not just visually approximated via matching column math. Known, accepted trade-off: `.loc-card` no longer stretches to a uniform row height — it sits in a flex column now, so it sizes to its own content — meaning a little natural height variance between cards in the same row is expected and fine.
  - `unlocked` is force-set to `true` when a landmark is passed to `LocationCard` here — History isn't a discovery/gamification context (the event text already names the place), so Explore's "???" mystery treatment never applies on this page, regardless of the requester's real check-in state.
  - Clicking a landmark card opens the real `LocationDetail` as an overlay on top of the History page itself (`HistoryPage` owns `openLandmarkSlug` state and renders `<LocationDetail>` directly, same pattern `RandomDrawPage` uses) — not a navigation to `/explore`.
  - Each event's `<h2>` title doubles as its Wikipedia link (new tab, no underline, only a hover color shift) when `wikipediaUrl` is set — replaced an earlier standalone "Wikipedia" text link below the summary (2026-08).
  - Sidebar era titles and left-border stripes are a flat `var(--gold)` everywhere, not the per-era `--era-accent` palette (`data/historyEras.js`'s `themeClass` → `.era-*` rules in `history.css`, which included a red and a purple) that used to color them. That per-era system is still defined in CSS but currently unread by anything — kept in case per-era theming comes back elsewhere, not dead code to delete casually.
  - **Chinese pixel-font gotcha:** title-level classes here (`.history-event__title`, `.history-sidebar__era-title`) use `font-family: 'Press Start 2P'`, a Latin-only font. Without a matching entry in `global.css`'s `html[data-lang="zh"] ...` override block, Chinese text in that class silently falls through to a generic `monospace` fallback — same declared `font-size`, but visibly smaller/thinner than the Latin pixel font renders, reading as "this heading has no presence." When adding a *new* class with this font-family that Chinese users will see, add it to that override block (font-family swap + a larger zh-specific `font-size`) in the same pass, not as an afterthought.
  - **Mobile horizontal-overflow gotcha (fixed 2026-08, worth understanding if it comes back):** `.history-layout`'s base rule sets `align-items: flex-start` — correct on desktop, where the layout is row-direction and it lets the sidebar and feed size their *heights* independently. The `@media (max-width: 820px)` block switches `.history-layout` to `flex-direction: column` but originally never touched `align-items` — so on mobile the cross axis becomes horizontal, and `flex-start` there means children shrink-to-fit their own content's preferred width instead of stretching to the container's 100%. `.history-feed` has no width of its own, so it was sizing itself to the widest thing inside it (in practice the 2-column `.location-grid` in related-landmark sections, wanting ~630–680px) and dragging the *entire page*, navbar included, into horizontal scroll — not just the History section, even though only History-specific content was the actual cause. Fixed by adding `align-items: stretch` inside that same mobile media query. A second, smaller contributor: CSS Grid/Flex items default to a content-based automatic minimum width that only zeroes out for boxes with their *own* non-visible `overflow` — `.loc-card` has `overflow: hidden`, but the `.history-landmark-item` wrapper around it (the actual `.location-grid` item now) didn't, so long location names could still inflate its track. Fixed with `min-width: 0` on `.history-landmark-item`. If mobile overflow ever resurfaces on this page, check both of these first — verify live with `document.body.scrollWidth` vs `window.innerWidth` plus a `getBoundingClientRect().right` sweep for offending elements (screenshots alone will mislead you: a page that's horizontally scrolled looks like content is "cut off on the left," which reads as a completely different bug from what it actually is).

## In progress — History Timeline, Era 1 content pass (2026-08-16)

Era 1 (`legends-origins`) is content-complete and live, now spanning **12 events** (grown from the original 7 during this pass), sorted by `startYear`: `early-history`, `libuse-prophecy`, `premysl-the-ploughman`, `founding-of-vysehrad`, `girls-war`, `seven-legendary-dukes`, `borivoj-first-duke`, `borivoj-conversion-and-exile`, `founding-of-prague-castle`, `bohemia-independence-895`, `st-ludmila-martyrdom`, `st-wenceslas-murder`. The five new events fill the ~140-year gap between the legendary founders and Bořivoj I (Bohemia's first historically-documented duke): the seven "blank" placeholder dukes Cosmas lists → Bořivoj as a Great Moravian vassal → his conversion/exile/restoration → the founding of Prague Castle itself → Spytihněv's 895 break from Great Moravia. Copy keeps the lighter "storybook" voice established for the original 7, and each new card's opening line still picks up from the previous card's ending — inserting cards mid-sequence meant going back to rewrite `borivoj-first-duke`'s hook (once `seven-legendary-dukes` landed before it) and `st-ludmila-martyrdom`'s hook + summary + `year` field, so the chain stays unbroken end to end. One factual error was caught and fixed in the process: `st-ludmila-martyrdom`'s `year` field said "874 (baptism)", but 874 was actually Bořivoj's *marriage* year — the real baptism (~883) is now correctly anchored by `borivoj-conversion-and-exile`.

Two other mid-session content corrections worth knowing if either event is touched again: `girls-war`'s original copy never named Vlasta (the actual leader of the women's revolt and founder of Děvín — Šárka was her subordinate, executing one operation on her orders) and wrongly said the women "refused to leave" when the legend has them proactively founding a rival stronghold; both fixed, with Děvín now linked as a related landmark. And `seven-legendary-dukes`' Horymír/Šemík anecdote is correctly attributed to Křesomysl's reign, not Neklan's, and sourced to Václav Hájek's 1541 chronicle, not Cosmas — Cosmas's original telling doesn't include Šárka, Horymír, or Vlasta at all; all three were folded in two centuries later by Dalimil/Hájek.

**Illustrations — 7 of 12 events have one:**
- Done: `early-history` → `/history/prehistory.webp`, `libuse-prophecy` → `/history/libuse.webp`, `premysl-the-ploughman` → `/history/premysl.webp`, `founding-of-vysehrad` → `/history/vysehrad.webp`, `girls-war` → `/history/sarka.webp`, `st-ludmila-martyrdom` → `/history/ludmila.webp`, `seven-legendary-dukes` → `/history/seven-dukes.webp`
- **Still needed:** `borivoj-first-duke`, `borivoj-conversion-and-exile`, `founding-of-prague-castle`, `bohemia-independence-895`, and `st-wenceslas-murder` have no `image` set. Once generated, drop the file in `client/public/history/` and set each event's `image` field to `/history/<file>.webp` — both in `seedHistoryEvents.js` *and* live via `HistoryEvent.updateOne({ slug }, { $set: { image } })`, since the seed script's `$setOnInsert` won't touch already-seeded docs (see **Adding new history events** above).
- **Working image-gen prompt template**, refined through several iterations — reuse it rather than starting from scratch: wide 16:9 (1024×576), aged tan parchment/scroll border with Celtic-knot corner flourishes, painterly high-detail pixel art (not blocky 8-bit), a single wide panoramic scene (not a "vision-within-a-vision" inset — that read as visually out of place and got dropped), and a bottom caption plaque described explicitly as *"a simple, perfectly flat rectangular parchment-colored bar... straight parallel edges... not tilted or in perspective"* (image models otherwise render it with an odd warped/perspective tilt) holding a **short** bold serif all-caps quote, with an explicit instruction to render it once with no repeated words (longer two-clause captions were prone to a word-duplication glitch). Offer 2–4 caption options via a question rather than guessing outright — the first pick has already been rejected once this pass. See the conversation this shipped in for exact copyable prompt text (`libuse.webp`, `sarka.webp`, `ludmila.webp`, `seven-dukes.webp`).
- **Fixed border/plaque dimensions (confirmed 2026-08-26, measured pixel-by-pixel off `egerland-endgame-1322.webp` at 1024×576):** border width is now a locked constant, **48px on all four sides** (~4.7% of canvas width / 8.3% of height) — state this explicitly in every future generation prompt rather than leaving border thickness to the model's own judgment, which is what produced the inconsistent widths across earlier cards. The bottom caption plaque's **height is likewise locked at 50px** (~8.7% of canvas height), sitting on the bottom border band and slightly overlapping into the scene above it, same as prior cards. The plaque's **width stays variable, sized to its caption text**: text width plus roughly 100px of padding on each side, capped at the ~928px safe interior width (1024 − 2×48px border) and never squeezed below ~350px even for a two- or three-word caption, so short captions don't look cramped. Spell out all four numbers (border 48px, plaque height 50px, plaque width = text + ~100px padding/side, min 350px) directly in the prompt text going forward, not just the qualitative description.
- If a generated image arrives as `.jpg` instead of `.webp`, this machine has no `cwebp`/`imagemagick`/`ffmpeg` on `PATH` — convert with Python + Pillow instead (`pip install pillow`, then `Image.open(...).convert('RGB').save(path, 'WEBP', quality=90)`). Use native Windows-style paths (`E:\...`) for the Python call, not Git Bash `/c/...`/`/e/...` paths — it's the Windows Python, not an MSYS one.
- **No white gap between border and scene (confirmed 2026-08-31):** generations would frequently leave a thin white/cream margin line between the inner edge of the parchment border and the illustrated scene itself — not part of the border's own aged texture, just an unwanted blank gap. State explicitly in every future prompt: *"the illustrated scene must extend directly to the inner edge of the parchment border, with zero gap — no white, cream, or blank margin line of any kind between the border's inner edge and the artwork itself; the scene's background should touch the border directly, edge to edge, like a page with no additional matting between the frame and the picture."* First caught and fixed on `the-great-bridge-1357.webp`.

**Related-landmark depth:** every Era 1 event now has at least one real related landmark (several previously had one or none). This pass added roughly a dozen new Locations specifically to support them — a Tetín / St. Ludmila cluster, a Vyšehrad-founding cluster (including the Neklanova Cubist house and Krliš hill, both tied to `seven-legendary-dukes`), and two "Before the Legend" prehistoric sites — plus linked several pre-existing-but-unseeded Locations (`st-georges-basilica`, `namesti-miru`, `basilika-sv-petra-pavla`, and a couple of user-submitted slugs). See **Adding new history events** above for the data-quality gotchas this surfaced (live-DB-only locations, a duplicate location, unexplained rarity/label drift) before the next batch.

Eras 2–6 are still untouched placeholders (`hasContent: false` in `data/historyEras.js`) — next content pass after images are done.

## In progress — History Timeline, Bohemian Duchy era content pass (2026-08-19/20)

Era 2 (`bohemian-duchy`) now has real content **through 1127**, chronological: `borivoj-first-duke` (870) … `bretislav-succession-law-1055`, `vysehrad-revival-1070` ("Brother Rivalry"/"兄弟争宠"), `vratislaus-emperors-fireman-1081`, `vratislaus-ii-first-crown-1085`, `duke-go-round-1092`, `bohemia-not-one-city-1101` (background card), `battle-of-chlumec-1126` ("The Bohemian War of Succession"/"波希米亚王位继承战争"), `what-was-moravia-1127` (background card). Everything through `bohemia-not-one-city-1101` has an image; `battle-of-chlumec-1126` and `what-was-moravia-1127` do not yet (the latter deliberately, per the user's spec for that card). **Not yet touched this pass:** `vladislaus-ii-second-crown-1158`, `judith-bridge-1170`, `otakar-hereditary-kingdom-1198` — content exists from an earlier pass but wasn't reviewed/re-fact-checked this time, and none of the three has an image.

**New feature built this pass — background-knowledge cards** (`cardType: 'event' | 'background'` on `HistoryEvent`, default `'event'`): a non-dated explainer aside slotted into the feed at a specific point for context a reader needs right there, distinct from a normal dated event. See the dedicated bullet under **Adding new history events** above for the full spec (rendering, sidebar-exclusion mechanics, when to proactively suggest one). Two now exist: `bohemia-not-one-city-1101` (Bohemia's scattered power centers before Prague was the obvious one) and `what-was-moravia-1127` (Moravia's shifting appanage/margraviate/Crown-land relationship to Bohemia, 1055–1247). Touches 4 frontend files (`HistoryEventSection.jsx`, `HistorySidebar.jsx`, `HistoryPage.jsx`, `history.css`) plus 3 new i18n keys (`history.backgroundLabel`/`collapseCard`/`expandCard`) — verified with a full `vite build` after wiring it up, not just `node --check` on the seed file.

**Two real pre-existing bugs found and fixed** while auditing Vratislav/Vladislav name usage (see the naming note below): `rotunda-sv-martina`'s ZH description said "弗拉迪斯拉夫二世" where EN/CZ said "Vratislav II" (ZH-only translation slip), and `romansky-most-vysehrad` said "King Vratislav I" in all three languages when it should be Vratislav II (no Bohemian ruler named Vratislav was ever crowned king except Vratislav II; the 10th-century Vratislav I was only ever a duke). Both fixed directly in the DB and the seed file.

**Recurring name-collision trap, worth repeating from the project memory:** "Vratislav" (Latinized Vratislaus) and "Vladislav" (Latinized Vladislaus) are two different men a century apart — Vratislav II (r. 1061–1092, first King of Bohemia in 1085) vs. Vladislaus II (r. 1140–1173, second "borrowed crown" in 1158, built the Judith Bridge's predecessor). This project's established ZH transliteration, checked against zh.wikipedia and used consistently throughout: **弗拉季斯拉夫** = Vratislav, **弗拉迪斯拉夫** = Vladislav (the only difference is 季 vs 迪). Don't let a casually-typed draft suggestion override this — it came up twice this pass as a proposed "fix" that would have actually been wrong.

**Four new Locations added this pass** (all `mythic`, all wired into `relatedLandmarks`): `peruc` (Oldřich & Božena legend), `lysa-nad-labem` (Jaromír's imprisonment/death), `chrudim` (where Břetislav I mustered troops and died, Jan 1055), `pomnik-premysla-orace` (Stadice, the legendary ploughing site). Also newly linked into `relatedLandmarks` for the first time despite already existing in the DB: `national-library-czech-republic` (Klementinum — holds the Vyšehrad Codex, made for Vratislav II's 1085 coronation) and `hradiste-libice` (site of both the 995 Slavník massacre *and* the 1108 Vršovci massacre — two purges, one hilltop).

**Voice note confirmed working well and now codified in CLAUDE.md** (see the **Voice** bullet above): short punchy sentences, concrete details kept, a dry closing line reframing the facts as a joke. The Langensalza/Gebhard aside in `vysehrad-revival-1070` is the model paragraph the user pointed to explicitly.

**Next up:** review/fact-check/image `vladislaus-ii-second-crown-1158` → `otakar-hereditary-kingdom-1198` (the era's last 3 events), then Eras 3–6.

## In progress — History Timeline, Rise of a Kingdom era split (2026-08-23)

What had been a single era, `kingdom-golden-age` (1199–1378, ~32 seeded events through `the-stepfather-problem-1288`), got split into two: `rise-of-a-kingdom` (1199–1345, keeping all the previously-seeded content — Otakar I through the Wenceslas II/Záviš of Falkenstein saga) and a narrowed `kingdom-golden-age` (1346–1378, currently empty, reserved for Charles IV's actual reign). See the **History Timeline** section above for the full rationale and mechanics. Practical note for whoever writes the Charles IV events: they're genuinely new content, not a continuation of the `rise-of-a-kingdom` chain — don't accidentally tag them with the old era key out of habit, and remember to flip `kingdom-golden-age`'s `hasContent` back to `true` once the first one is seeded.

Current narrative status of `rise-of-a-kingdom`, in order: Otakar I's marriage maneuvering → Otakar II's rise and territorial peak (Sudetes to the Adriatic) → the Nuremberg ban, the Vítkovci revolt, and the Austria renunciation (1276) → Rudolf of Habsburg elected, the Habsburg background card → the Marchfeld defeat and death (1278) → the guardianship crisis (Otto of Brandenburg/Rudolf/Henry Probus splitting up the kingdom, the Bezděz and Spandau imprisonments) → the 1282 famine and 1283 return to Prague → the long-delayed Wenceslas II/Guta of Habsburg marriage (1285, at Cheb) → Kunigunda's death and Záviš of Falkenstein's continued, self-sustaining rise (1285–1288) → Záviš's arrest and execution (1289–1290) → John of Luxembourg's 1310 seizure of Prague, the Speyer wedding and 1311 coronation, and the three-way tug-of-war between his father's Luxembourg/French-style advisors, Elizabeth's Přemyslid ambitions, and a nobility that wanted neither, culminating in the April 1315 dismissal of his foreign advisors (`capital-changed-hands-thrice-1310`, `dawn-of-the-luxembourgs-1310`, `a-king-who-spoke-no-czech-1311`, `a-court-at-war-with-itself-1313`) → Henry of Lipé's own proxy war against Elizabeth via Queen-dowager Eliška Rejčka (`the-two-queens-proxy-war-1315`) → the 1319 Loket raid and Elizabeth's estrangement from John (`a-marriage-falls-apart-1319`) → John's drift into near-total disengagement from Bohemia (`a-king-who-stopped-caring-1319`) → the Mühldorf campaign and Egerland acquisition (`egerland-endgame-1322`) → John's 1320s–30s career as itinerant diplomat/condottiere culminating in the 1331 Italian lordships (`diplomat-king-1331`) → the 24 August 1335 Treaty of Trentschín and its November 1335 Congress of Visegrád confirmation: John sells his stale, decades-old Přemyslid-derived claim to the Polish crown to Casimir III for 20,000 kopy of Prague groschen, Casimir renounces Silesia to Bohemia permanently (most duchies become Bohemian fiefs, Wrocław/Głogów escheat under *direct* crown rule after Duke Henry VI's heirless death that same November), and the resulting border holds with only minor changes until after WWI (`the-border-that-stuck-1335`) → John's 1336 Lithuanian Reise (not his first — he'd already ridden once before, in 1329), the onset of his blindness, and — folded into this same card at the user's request on 2026-08-26 rather than split into a separate future card, since the card felt thin on its own — the two doctors' fates: a French physician's failed treatment earning him a sack in the Oder at Wrocław on John's own order, then an Arab physician summoned to Prague who finished blinding the right eye, both scenes sourced to genuine Zbraslav Chronicle quotes (`crusader-king-loses-his-sight-1336`, two `quotes[]` entries; now has one image, caption "TRIUMPH AND PAIN" — a winter battlefield scene, John in the bearded/~40 look front and center, hand over his right eye, expression a deliberate mix of victory and pain; took 3 generation passes to get the routed Lithuanian/Rus' army in the background actually fleeing away rather than reading as a second wave charging in — worth restating that direction explicitly in similar battle-aftermath prompts going forward). The card ends with the right eye gone for good and a light, unspoiled forward-hook to the left eye's own loss "a few years yet" later (~1339–1341) — **that part is still genuinely unwritten**, save it for whenever the timeline reaches it rather than assuming this card already covers it → John's 1337 alliance with his own childhood friend Philip VI of France at the outbreak of the Hundred Years' War (the two grew up together at the French court as boys, neither destined to be king at the time), his 30 November 1338 appointment as governor of Languedoc — barely two years after losing his right eye — commanding roughly 12,000 troops, taking Penne/Puyguilhem/Blaye/Bourg, and failing to take Bordeaux itself in July 1339 before supply problems forced a withdrawal (`an-alliance-with-france-1337`, quote from Guillaume de Machaut's *Confort d'Ami* — the earliest reference in this timeline to Machaut, who'd go on to serve John for decades) → a background card explaining the Hundred Years' War itself (cause: Edward III's claim to the French throne through his mother Isabella, rejected by French succession law, versus Philip VI's own male-line claim; shape: 116 years on and off, Crécy/Poitiers/Agincourt early English wins, Joan of Arc's 1430s turnaround, French victory in 1453), kept deliberately brief per the user's explicit request on 2026-08-26 — this is not the point of the timeline, just enough context for readers before moving on (`what-was-the-hundred-years-war-1337`, `cardType: 'background'`, 2 paragraphs only, much shorter than earlier background cards like `what-were-the-crusades-1147`; a spoiler line originally in its first draft, naming Crécy as where John himself dies, was cut at the user's request — don't reintroduce it) → the standoff between John and young Charles resolved, 1329–1341, in a full rewrite the user dictated on 2026-08-26 that kept the original 1333-card's content entirely intact and layered new material around/after it rather than replacing anything: Jindřich of Lipá's August 1329 death leaves a power vacuum, Bohemia's nobility fractures into competing power grabs through the early 1330s, a Luxembourg-loyalist noble faction arranges Charles's October 1333 return (**per the user's account John didn't know in advance — flagged to the user as contradicting the one secondary source found, stavitele-katedral.cz, which says John consented to Charles leaving Merano; not independently resolved, so treat "did John know" as genuinely unsettled between sources, not confirmed either way**), the 1334 Margrave of Moravia appointment now explicitly named as the start of the "Luxembourg dvojvládí" (dual rule), continued father-son tension even as Charles proved himself competent, and finally an 8 June 1341 diet session (Czech-language sources give 11 June for a related proclamation; German sources give 8 June for the administrative handover — this card just says "June 1341" to sidestep the discrepancy) where John, now effectively unable to rule in person, formally announced Charles as heir to the Bohemian throne — not a coronation, which still waited for John's 1346 death. → Charles's Tyrol regency lost to the Wittelsbachs (Margaret of Tyrol expels his brother John Henry in Nov 1341, remarries the Emperor's own son), the resulting father-son rupture that took Charles's own former childhood tutor, now Pope Clement VI, to mediate in 1342, and John's quiet June 1342 renunciation of fealty to Emperor Louis IV that would matter four years later (`father-and-son-at-odds-1342`) → Prague's own slow rebuilding, 1338's citizen-funded Old Town Hall charter alongside the bigger 21 November 1344 gesture: Prague's elevation to an archbishopric and John, Charles, and John Henry laying St. Vitus Cathedral's Gothic foundation stone together with Prague's first archbishop, Arnošt of Pardubice (`dawn-over-prague-1344`, the first card to link the now-fixed `st-vitus-cathedral` slug, alongside `old-town-hall`) → a background card explaining the bishopric/archbishopric distinction (parish → diocese/bishopric → ecclesiastical province/archbishopric, suffragans, metropolitan authority), grounding it in Prague's own case: bishopric since 973 but a Mainz suffragan the whole time (hence needing to import the Archbishop of Trier for Vratislaus II's 1085 coronation), freed by Clement VI's 30 April 1344 bull *Ex supernae providentia maiestatis*, gaining its own suffragans (Olomouc, plus the newly-created Litomyšl) — with a light, unnarrated forward-hook to September 1347, when Prague's own new archbishop (Arnošt of Pardubice) would crown Charles himself (`bishopric-and-archbishopric-1344`, `cardType: 'background'`, `startYear: 1344.1`) → the Luxembourg family's real campaign for the title King of the Romans, led by Charles's great-uncle Baldwin of Luxembourg (Archbishop of Trier since 1307) in exchange for serious formal promises and a bill that topped 250,000 florins, Clement VI's April 1346 deposition of Louis IV, and the 11 July 1346 election at Rhens where five of seven electors (Baldwin, Gerlach of Mainz, Walram of Cologne, Rudolf of Saxony, and John himself, voting for his own son) made Charles King of the Romans while Louis IV still held on (`a-day-worth-remembering-1346`, `startYear: 1345.9` to sort correctly before the August death card at flat `1346`) → Renamed from `the-heir-who-forgot-his-language-1333` to `the-prince-who-came-to-put-out-the-fire-1341` (slug, `year` field, and `startYear` all now consistently 1341) — the old slug/year mismatch flagged in this file's own earlier revision is now resolved, don't reintroduce it. Now has two images: the 1333 ruined-Prague-Castle arrival (caption "THE PRINCE BACK HOME", reusing the young-Charles locked design) and the June 1341 diet proclamation (caption "THE PROMISE OF A CROWN", Charles aged up to ~25, John now ~45 with the eyepatch), generated 2026-08-26. **Also added 2026-08-26 (user-flagged gap):** a new paragraph between the France-childhood paragraph and the noble-arranged-homecoming paragraph, covering Charles's Italian apprenticeship the card had skipped entirely — 1331 campaigning alongside John in Italy, an October 1332 battlefield win commanding 5,000+ troops against an Este/della Scala/Gonzaga coalition, and early-1333 governing Lucca in Tuscany in John's name (founding the fortress town of Montecarlo, "Charles's Mountain," nearby) before John pawned Lucca away to the Rossi of Parma later that same year. Reframes the "no father, no mother" homecoming that follows: he arrives in October 1333 not as a sheltered kid but as someone who'd already led troops, won a battle, and run a city. John's death at Crécy is now written (`the-wandering-knight-kings-legacy-1346`, added 2026-08-26 at the user's explicit direction as "the last event of this era") — the user chose to keep it as `rise-of-a-kingdom`'s own closing bookend rather than opening `kingdom-golden-age`, since it's fundamentally about closing John's story (his death, legacy, burial saga) rather than opening Charles's own reign as king. `rise-of-a-kingdom`'s own `yearRange` in `data/historyEras.js` was corrected accordingly to 1199–1346 (was 1199–1345); `kingdom-golden-age`'s stays 1346–1378 unchanged, so the two eras now deliberately share 1346 as a boundary year. `kingdom-golden-age` stays empty (`hasContent: false`) until actual Charles-IV-as-king content exists — the next card written should be the first real one there, not this one. Total blindness is now dated to 1339 in this card's opening line (per the user), closing the "still-unwritten loss of his left eye" gap flagged earlier — no separate dramatic doctors-of-the-left-eye card was written or is planned unless the user asks. Content: the Crécy battle itself (first real pitched battle of the Hundred Years' War, lopsided English/French casualties), three quotes (his own Latin dying declaration per Beneš Krabice of Weitmil's *Cronica ecclesiae Pragensis*; Froissart's famous tied-reins account of the actual charge and death, supplied by the user in Czech and translated into EN/ZH here; Machaut's memorial verse, also user-supplied in Czech), the same-calendar-date coincidence with Otakar II's 1278 Marchfeld death (68 years apart, both already-covered Bohemian kings), and — as a closing beat — John's genuinely eventful posthumous journey (Altmünster → Neumünster → hidden by the Boch family, of Villeroy & Boch fame, during the French Revolution → a 19th-century Prussian chapel at Kastel-Staadt → finally repatriated to Luxembourg's Notre-Dame Cathedral crypt after WWII, where he still rests). Title deliberately echoes "the legend of Bohemia's wandering knight-king" phrase already used in `the-prince-who-came-to-put-out-the-fire-1341`. Closes on Charles simply picking up the crown the June 1341 assembly had already promised him.

**Not yet written:** the Bautzen (1319) and Görlitz (1329) Upper Lusatia acquisitions already got a passing mention inside `diplomat-king-1331`'s prose rather than their own card, so still technically open if anyone wants to circle back; Charles's rebuilding of Prague Castle and the 1344 founding of St. Vitus Cathedral/elevation of Prague to an archbishopric belong to `kingdom-golden-age` now, as does the rest of Charles's actual reign as king (1346–1378) — genuinely new content, not a continuation of this chain. The 1335 territory shift (Silesia formally joining the Bohemian Crown lands) is a genuine territory-map moment per [[feedback_bohemia_territory_map]] — done: `the-border-that-stuck-1335` now has two images, the Visegrád banquet (caption "THE TREATY OF TRENČÍN") and a `-map.webp` territory map matching the established `sudetes-to-adriatic-1272-map.webp`/`baltic-to-danube-1301-map.webp` style (caption "SILESIA JOINS THE CROWN"), generated 2026-08-26.

**Landmark sweep, 1335–1341 (2026-08-26):** searched the live Location DB for anything worth adding as `relatedLandmarks` across `the-border-that-stuck-1335` → `crusader-king-loses-his-sight-1336` → `an-alliance-with-france-1337` → `the-prince-who-came-to-put-out-the-fire-1341`. The first three happen entirely outside the Czech lands (Trenčín/Visegrád in Hungary, Lithuania/Wrocław, France) — genuinely nothing to link, not a gap. `the-prince-who-came-to-put-out-the-fire-1341` got `krivoklat-castle` added (already existed in the DB, already used once before in `a-marriage-falls-apart-1319` for the same young-Wenceslas-in-custody moment the 1341 card's own text now explicitly names — the relation caption calls back to the card's own "already appeared twice" line).

**Brno added as a Location (2026-08-26), fulfilling the "reserved for later" placeholder** left in `olomouc`'s own seed-file comment (2026-08-24) — same third exception (barely-a-Prague-day-trip + outsized standalone historical weight), written in the same long-form jihlava/plzen/olomouc template (intro — history — landmarks — bonus, no sub-landmark splitting). `slug: 'brno'`, `rarity: 'legend'`, `labels: ['city']` (matches the *live* label array actually used by plzen/olomouc/jihlava today, not the `['city', 'historical']` written in their own seed-file source — those two have drifted apart; see the standing "don't assume the DB matches what you seeded" gotcha elsewhere in this doc). Content covers: Špilberk Castle (built ~1270 by Otakar II, became the Margraviate of Moravia's actual seat — direct tie-in to Charles's own 1334 title from `the-prince-who-came-to-put-out-the-fire-1341`), the 1428/1430 Hussite sieges withstood, Brno inheriting Moravia's capital status when Olomouc fell to Sweden in 1642 (echoes/completes the line already in `olomouc`'s own card), the 1645 Swedish siege and the Petrov Cathedral noon-bells-rung-at-eleven legend, the Old Town Hall's dragon/wheel/crooked-pinnacle legends, Špilberk's later life as a notorious Habsburg political prison, Villa Tugendhat (UNESCO 2001, and where the 1992 Czechoslovak dissolution agreement was actually signed), and a bonus about the 50,000-body Brno Ossuary discovered under Zelný náměstí in 2001. No pixel art yet — emoji fallback until provided, per [[feedback_pixel_art]]. Slavkov u Brna/Austerlitz is still deliberately not built — revisit only once the Napoleonic era is reached.

**Major restructuring, 2026-08-26 — `the-prince-who-came-to-put-out-the-fire-1341` split in two.** Every mention of that slug in the paragraphs above is now stale; here is the corrected picture. The user's own diagnosis: cards positioned chronologically *before* it (`the-border-that-stuck-1335`, `crusader-king-loses-his-sight-1336`) were already treating Charles as an established character ("the son who'd already become margrave... we mentioned him back then as Wenceslas, but now he's got a different name") before the card that actually introduces him had been read in feed order — a real reader-facing inconsistency, not just a style question. Fix: split the card at its existing natural paragraph break (right after "clawed back the castles and crown estates...") into two separate cards, and move the first half chronologically earlier:
- **`the-prince-who-came-to-put-out-the-fire-1333`** (renamed from the old `-1341` slug, `startYear: 1341` → `1333`, now sits between `diplomat-king-1331` and `the-border-that-stuck-1335`): everything through Jindřich of Lipá's death, Charles's childhood/Italy/Lucca years, the October 1333 return, forgetting Czech, becoming Margrave in 1334, and clawing back castles. Both quotes and both `relatedLandmarks` (`krivoklat-castle`, `brno`) stayed with this half. Its "already made two quiet appearances" callback was trimmed to "one," since only the 1319 Loket appearance now precedes it in feed order — the 1335 appearance hadn't happened yet at this position. Kept the original image (`the-prince-who-came-to-put-out-the-fire-1333.webp`, physically renamed from the `-1341` file).
- **`blindness-not-affection-1341`** (new slug, stays at `startYear: 1341`, sits where the old combined card used to be, right before `father-and-son-at-odds-1342`): just the father-son tension, the June 1341 diet, and the "John kept the reputation, Charles kept the actual job" closer, rewritten with its own self-contained opening paragraph rather than dangling a pronoun back into content that's now a different card. No quotes, no landmarks. Kept the second image (`blindness-not-affection-1341.webp`, physically renamed from the `-1341-promise.webp` file).

**Downstream fixes made in the same pass:** `the-border-that-stuck-1335` no longer teases Charles's name — it now opens with a direct bridging paragraph ("John didn't make the trip alone. Charles came with him...") and the Trenčín-meeting paragraph just says "Charles" instead of "the son who'd already become margrave... a different name." Charles's own presence was also added (verified via WebSearch, not just assumed) to three cards that already existed but hadn't mentioned him: he accompanied John on the 1336 Lithuanian Reise (`crusader-king-loses-his-sight-1336`), rode with him into the 1338–39 Gascony campaign (`an-alliance-with-france-1337` — this one is the least independently verified of the three, only indirect/circumstantial support found despite two search passes, flagged to the user rather than silently asserted), and fought at Crécy itself and was wounded badly enough to need pulling from the field, surviving where his father didn't (`the-wandering-knight-kings-legacy-1346` — also adjusted its "Charles didn't wait long" closing line to note he had to recover from that wound before traveling back to Prague). **Gotcha hit during this edit:** one large Edit call silently corrupted straight quotes into curly smart quotes across an entire replaced block (JS syntax error on `node --check`) — cause unclear, possibly an artifact of the replacement text's length; caught immediately by the syntax check this project always runs before syncing, fixed by retyping the block with explicit straight quotes. Worth re-running `node --check` after *every* large Edit on this file, not just before the final DB sync, since this kind of corruption is silent until you check.

**Young John of Luxembourg — locked character design for illustration-prompt consistency (confirmed 2026-08-25):** fair/light-brown hair worn chin-length and parted, a slender youthful build, clean-shaven, dressed simply but nobly rather than in full regalia (a dark blue tunic with gold trim in the reference image) — established from the Speyer wedding illustration (caption "THE WEDDING OF LUXEMBOURG AND PŘEMYSLID", for `dawn-of-the-luxembourgs-1310`). Reuse this description in future prompts depicting John at a young age (his teens through early twenties, i.e. anywhere in the 1310s) so he reads as the same person across cards, the same way the "no Gothic spires before 1344" rule already keeps the architecture consistent.

**Henry of Lipé — locked character design for illustration-prompt consistency (confirmed 2026-08-25):** dark hair greying at the temples, a weathered, watchful, clean-shaven face, tall and imposing build, dressed in a dark charcoal-grey high-collared long tunic with a brown leather belt — no ostentation, just quiet authority. Established from the "WAR BETWEEN TWO QUEENS" illustration for `the-two-queens-proxy-war-1315` (the arms-crossed figure on the right). A first pass at illustrating this same card's other two images (the Týřov arrest, the Domažlice outcome) rendered Henry inconsistently across all three — an older white-haired man in one, someone not clearly Henry at all in another — before this description got locked down; regenerate any card depicting Henry against this description rather than improvising a new look each time.

**Elizabeth of Bohemia (Eliška Přemyslovna) — locked character design for illustration-prompt consistency (confirmed 2026-08-25):** dark hair, dignified but plainly dressed for a secluded country residence rather than full court regalia — a fur-trimmed dark wool gown, hair covered by a simple veil — with an expression that reads as alarmed but proud rather than merely frightened. Established from the `a-marriage-falls-apart-1319` illustration (the Loket Castle raid scene, watching from a window with her children drawn close). Reuse this description in future prompts depicting Elizabeth so she reads as the same person across cards, the same way John's and Henry of Lipé's locked designs already do — she's the first card where she's been the actual illustration protagonist, so this is the baseline going forward rather than a one-off.

**Louis of Bavaria (Ludvík Bavorský / Ludwig der Bayer) — locked character design for illustration-prompt consistency (confirmed 2026-08-25):** dark hair and a full dark beard, roughly a decade older than John (John's own regular ally since brokering the 1318 Domažlice peace), a weathered and commanding presence, dressed in Bavarian blue-and-white lozenge colors under a fur-trimmed mantle. Established from the `egerland-endgame-1322` illustration (the Mühldorf battlefield scene, clasping John's arm in open camaraderie after the victory). Reuse this description in future prompts depicting Louis so he reads as the same person across cards, the same way John's, Henry of Lipé's, and Elizabeth's locked designs already do.

**Middle-aged John of Luxembourg (1330s onward) — locked character design, distinct from the "Young John" 1310s entry above (confirmed 2026-08-26):** light brown/dirty-blond wavy hair worn shoulder-length, a short trimmed beard with a little grey at the temples (he's now ~39–40), still dressed richly in a dark navy tunic with gold trim, a slightly more weathered and imposing presence than his younger self but not yet visibly blind (that's `crusader-king-loses-his-sight-1336`, one year after this). Established from the `the-border-that-stuck-1335` banquet illustration (the Visegrád feast, seated beside Charles Robert of Hungary). Use the *clean-shaven* `diplomat-king-1331` look for anything set 1331 or earlier, and this bearded version for 1335 onward.

**Young-adult Charles (as Margrave of Moravia, late teens, 1330s) — locked character design (confirmed 2026-08-26):** chestnut-brown hair worn short, clean-shaven, slighter/slimmer build than his father, dressed in a fine wine-red tunic trimmed with ermine — visually distinct from John's navy-and-gold. Established from the same `the-border-that-stuck-1335` banquet illustration (seated beside John, across from the Hungarian and Polish delegations) — this is Charles's first actual generated illustrated appearance in this timeline (an earlier prompt for `the-prince-who-came-to-put-out-the-fire-1341`/then-`the-heir-who-forgot-his-language-1333` proposed a similar look but was never generated). Reuse this for Charles at a similar age (late teens to early twenties, roughly 1333–1341) in future cards.

**`the-wandering-knight-kings-legacy-1346` — images done (2026-08-26), the true closing illustration for `rise-of-a-kingdom`:** "THE LAST CHARGE" (John's blind charge with reins tied together, took three regeneration passes — first had his eyes visibly open despite the "blind" premise, second left a stray "48px" border-spec annotation baked into the artwork itself, third rendered the Luxembourg banner as a flat modern red-white-blue tricolor instead of the real barry-silver-and-blue-with-a-red-lion coat of arms; all three fixed by making each requirement maximally explicit in the prompt rather than assuming the model would infer it) and "HIS CITY, HIS KINGDOM" (Charles, crowned and in the age-28–40 locked look, gazing over Prague at golden hour — deliberately symbolic rather than a literal 1346 snapshot, since his real Bohemian coronation wasn't until September 1347; the user knowingly kept a stone bridge visible in the skyline despite Charles Bridge not existing yet either, reasoning it reads as Charles looking at the city he's about to go on and build, not a documentary error). **Lesson for future battle-scene prompts on this project:** state "no readable text/scale-marker/annotations anywhere in the artwork" explicitly — border-spec numbers like "48px" can otherwise leak from the prompt straight into the rendered image.

**Prague cityscape (distant/skyline view) — established look, worth treating as a loose visual anchor going forward (confirmed 2026-08-26):** red-tiled rooftops climbing the hill, the Vltava catching golden light, a stone arch bridge across the river, St. Vitus Cathedral's scaffolded silhouette on the castle hill above the rest of the skyline. Established from `the-wandering-knight-kings-legacy-1346`'s "HIS CITY, HIS KINGDOM" illustration. The user kept the bridge deliberately even knowing Charles Bridge doesn't exist yet at this point in the story (1346) and the older Judith Bridge had already been damaged by the 1342 floods — reasoning that re-prompting to fix one small anachronism risks degrading the rest of an already-good, increasingly consistent city view, and that it reads fine as Charles looking at the city he's about to go on and build rather than a documentary snapshot. Don't second-guess that call in future prompts; treat the skyline as roughly settled unless the user raises it again. **Resolved for 1357 onward (noted 2026-08-31):** now that `the-great-bridge-1357` has actually been written (bridge construction starts 1357, alongside St. Vitus Cathedral's own ongoing construction since 1344 per `dawn-over-prague-1344`), any skyline scene set in 1357 or later can show both under construction together with zero anachronism hedging needed — the earlier "known, accepted trade-off" framing only applies to scenes set before 1357.

**Charles in his prime, age ~28–40 (confirmed 2026-08-26, user's own explicit call) — locked character design, supersedes the young-margrave look above for this age range:** chestnut-brown hair worn short with a slight wave and side part (neater/more styled than the margrave look), clean-shaven, sharp and determined features, a deep wine-red robe with white ermine trim and a gold-trimmed collar, rust-orange sleeve visible underneath. Established from `a-day-worth-remembering-1346`'s second illustration (the private Baldwin arm-clasp scene, caption "THE PRICE OF A CROWN") — use this for any card depicting Charles **before** his 22 April/5 April 1355 imperial coronation in Rome. Superseded from that point on by "Emperor Charles," below.

**Emperor Charles IV, post-coronation (confirmed 2026-08-31) — locked character design, supersedes the wine-red "in his prime" look above for anything dated from the 1355 Rome coronation onward:** same face and hair (chestnut-brown, wavy, clean-shaven, sharp features), but now dressed in a rich ivory/cream ceremonial mantle covered in elaborate gold brocade embroidery (geometric/floral medallion patterns), gold trim, a jeweled collar — not the earlier wine-red-and-white-ermine combination, which is retired for any scene set after the coronation. Established by extending `crowned-in-the-eternal-city-1355`'s own coronation illustration (the user's explicit direction: "延续这个" — continue this design, rather than inventing a separate new imperial-purple scheme that had been proposed first). First reused in `the-golden-bull-1356`'s electors-scene illustration, which was regenerated specifically to swap out the pre-coronation look once this rule was confirmed — a real instance of "don't assume the DB/art matches what you seeded a scene ago," this time for character design rather than data. Charles will still need a further-aged version once he's noticeably older (his reign runs to 1378).

**Older, totally blind John of Luxembourg (1339 onward) — locked character design, supersedes the "middle-aged/eyepatch" look for anything dated 1339 or later (confirmed 2026-08-26):** about forty-eight, hair fully grey now (not just light brown with grey at the temples), a fuller grey beard, both eyes closed in a calm, sightless expression — not an eyepatch anymore, since he lost the second eye by 1339 (see `crusader-king-loses-his-sight-1336`'s own text). Established from `dawn-over-prague-1344`'s illustrations. Use the bearded-with-one-eyepatch version (established from `the-border-that-stuck-1335`/`an-alliance-with-france-1337`) for anything set 1336–1338; this fully-blind, both-eyes-closed, greyer version for 1339 onward.

**Arnošt of Pardubice — locked character design (confirmed 2026-08-26):** late forties, dignified and scholarly-looking, bishop's mitre and rich vestments (gold and burgundy). Established from `dawn-over-prague-1344`'s two illustrations — Prague's first archbishop, so this design will likely recur through the rest of his tenure.

**John Henry (Jan Jindřich), Charles's younger brother — locked character design (confirmed 2026-08-26):** early twenties, dark hair, dressed more simply than Charles (plain blue rather than ermine-trimmed red), a slighter build. Established from `dawn-over-prague-1344`'s ceremony illustration — his first appearance in this timeline's illustrations, though he's been named in prose since `father-and-son-at-odds-1342` (the Tyrol marriage/expulsion).

**Baldwin of Luxembourg (Charles's great-uncle, Archbishop of Trier) — locked character design (confirmed 2026-08-26):** elderly (in his sixties by 1346), white hair, a deeply lined but commanding face, dark purple-trimmed archbishop's vestments — deliberately distinct from Arnošt of Pardubice's gold-and-burgundy. Established from `a-day-worth-remembering-1346`'s two illustrations (the Rhens election, caption "FIVE VOTES AT RHENS", and the private arm-clasp/promise scene, caption "THE PRICE OF A CROWN").

**Pope Clement VI — locked character design (confirmed 2026-08-26):** mid-fifties, clean-shaven with sharp, intelligent features, white and gold papal vestments with a jeweled tiara. Established from `father-and-son-at-odds-1342`'s illustration (the Avignon reconciliation scene, one hand on John's shoulder, the other extended toward Charles, caption "PEACE AT AVIGNON") — his first illustrated appearance in this timeline, fitting since this is also the first card to actually show him on the page despite being introduced in prose as Charles's own former French-court tutor.

**Philip VI of France — locked character design (confirmed 2026-08-26):** dark hair, a short full dark beard, mid-forties, dressed in rich royal blue robes trimmed with gold fleur-de-lis. Established from `an-alliance-with-france-1337`'s illustration (the arm-clasp/handshake greeting scene, captioned "TWO KINGS, ONE FRIENDSHIP") — his first illustrated appearance in this timeline. Also note: this same image is the first to show John wearing the black leather eyepatch over his right eye (lost in `crusader-king-loses-his-sight-1336`) — carry the eyepatch forward on the bearded/~40 John look in any card from 1336 onward, on top of the existing hair/beard/navy-tunic description.

**Blanche of Valois, Charles's first wife — locked character design (confirmed 2026-08-26):** young (matching Charles's own age, ~18 in 1334), fair features, dressed in elegant 1330s French Gothic court fashion — a fitted, richly embroidered gown (soft blue with gold trim), hair covered by a fashionable French wimple-and-veil headdress, visibly more ornate than anything Bohemian courtiers wear, deliberately marking her as foreign at court. Established from `so-much-to-rebuild-1334`'s illustration (Charles welcoming her and her Luxembourg/French retinue at the Prague Castle gate, caption "WELCOME TO PRAGUE, BLANCHE") — her first illustrated appearance in this timeline, paired there with the existing young-adult/margrave Charles design (chestnut-brown hair, clean-shaven, wine-red ermine-trimmed tunic).

## Deployment

- Frontend → Vercel (root set to `client/`)
- Backend → Render (`prague-stories-api.onrender.com`)
- Images → Cloudinary (`prague-stories/covers/<slug>`)
- DB → MongoDB Atlas
